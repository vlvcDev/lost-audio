[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pedal-catalog"
version = "0.1.0"
description = "Local NAM catalog and TONE3000 integration for Pedal V0"
requires-python = ">=3.11"

[project.scripts]
pedal-catalog = "pedal_catalog.cli:main"
